pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Running Python script..."
                sh 'python3 test.py'
            }
        }
    }
}
